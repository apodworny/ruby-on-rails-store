<%= form_for(@front_end) do |f| %>
  <% if @front_end.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@front_end.errors.count, "error") %> prohibited this front_end from being saved:</h2>

      <ul>
      <% @front_end.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
